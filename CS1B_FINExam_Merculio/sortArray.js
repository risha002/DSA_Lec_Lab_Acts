/*
let numbers = parent(prompt("Enter some numbers: "));
let names = prompt("Enter name: ");

function reverseNum(int) {
    return 
}
*/

/*
let input = prompt("Enter a list of items separated by commas:");
let array = input.split(",");

console.log(array);
*/

let numberInput = parseInt(prompt("Enter numbers: "));
let namesInput = prompt("Enter names: ");
let merged = numberInput.concat(namesInput); //merged both arrays
